language: java
jdk:
 - openjdk8
 - openjdk11
notifications:
  email:
    recipients:
      - jnauro1@studentstowson.edu
    on_success: always # default: change
    on_failure: always # default: always
